package fr.dauphine.javaavance.td1;

public class TestPoint { 
	

		// Exercice 2
		
		
		// 2. We can't access directly to variables of the class Point because their field are "private".
		/*public static void main(String[] args) {
		Point p=new Point();
		System.out.println(p.getX() + " " + p.getY());
		
		// It's necessary to set getters of x and y in "System.out.println(x + " " + y) instead of x et and y".
		
		// 3. We don't want to access directly to variables of the class.
		
		// 4. An accessor is a method to access to variables who they are in private.
		//	  And we had to do it here in "System.out".
		
		// 5. The problem is that the variable's name doesn't begin with p.
		
		// 6. So, with x and y, we need to set "this." before x and y in order to make the difference between argument and variable of class.
		
		// 7. We need to define a static variable and set ++ like that: "nbPoints++;" in each constructors.
		System.out.println(Point.nbPoints);
	}*/
	
	
}
